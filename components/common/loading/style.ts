'use client';

import styled from '@emotion/styled';

export const LoadingContainer = styled.div`
    display: flex;
    gap: var(--spacing-xs);
    align-items: center;
    justify-content: center;
`;
